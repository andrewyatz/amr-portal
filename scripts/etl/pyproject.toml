[project]
name = "etl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.3.2",
    "jsonschema>=4.25.0",
]

[project.optional-dependencies]
yaml = [
    "pyyaml>=6.0.3",
]
